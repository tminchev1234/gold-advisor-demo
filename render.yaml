# Render Blueprint — Gold Advisor demo (static site).
services:
  - type: web
    name: gold-advisor-demo
    runtime: static
    buildCommand: ""
    staticPublishPath: .
    pullRequestPreviewsEnabled: false
